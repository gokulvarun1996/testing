version: 0.0
os: linux
files:
  - source:/testing
    destination: /var/www/html/
permissions:
  - object: /var/www/html/testing
    owner: root
    group: root
    pattern: "*"
    mode: 775
    type:
      - file
      - directory
hooks:
  BeforeInstall:
    - location: /codeclear.sh
      runas: root
  AfterInstall:
    - location: /codepull.sh
      runas: root
